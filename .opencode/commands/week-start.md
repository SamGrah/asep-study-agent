---
description: Start study week N — foundation cards + cold pretest
---
Start study week $ARGUMENTS.

1. Read study-plan.md to confirm week $ARGUMENTS sections and check off admin items due.
2. Generate this week's foundation cards (mochi-cards skill: ~15–30 atomic cards,
   tags [foundation, week-$ARGUMENTS, <topic>], status: ready) for every section listed —
   key verbatim definitions, each process's purpose + key inputs/outputs, and the
   classic exam distinctions. Run `tools/mochi/mochi lint` then `build -week $ARGUMENTS`.
3. Then PRETEST me cold (testing effect): quiz me through the new foundation cards
   one at a time BEFORE I do the reading. Don't teach first. Track what I miss and
   summarize my weak spots at the end so the reading is targeted.
